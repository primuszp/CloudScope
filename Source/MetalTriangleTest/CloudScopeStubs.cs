// Stub types so MetalTriangleTest can compile MTKEventView without CloudScope dependency
namespace CloudScope
{
    public enum ViewerMouseButton { Left, Right, Middle }
    public enum ViewerKey { Unknown }
}
